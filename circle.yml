machine:
  python:
    version: 3.4.4

database:
  pre:
    - mysql -e 'CREATE USER root IDENTIFIED BY password'

dependencies:
  pre:
    - pip install pylint

test:
  post:
    - pylint tap_mysql -d missing-docstring,invalid-name,too-many-locals,too-few-public-methods,fixme
